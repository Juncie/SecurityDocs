import { View, Text } from 'react-native';
import React, { useState } from 'react';

const CustonToggleButton = () => {
	return (
		<View>
			<Text>CustonToggleButton</Text>
		</View>
	);
};

export default CustonToggleButton;
