import React from "react";
import { Button } from "react-native";
import styled from "styled-components";

// Colors
export const Colors = {
  primary: "#3177FF",
  secondary: "#eeeeee",
  brand: "#efefef",
  black: "#000000",
  white: "#ffffff",
};

const { primary, secondary, brand, black, white } = Colors;
